# 계획 검토 01 — 2026-05-18

> **목적**: 구현 시작 전 전체 기획 문서(regulatory.md / prd.md / spec.md / plan.md / task.md / CLAUDE.md / .claude/rules / docs/adr / docs/superpowers/plans)의 오류·불일치·미결 사항을 전수 점검한다.  
> **검토 범위**: 기술 스택 버전, 스키마 설계, 기능 범위, 규제 정보, 구현 계획, 규칙 파일, ADR  
> **완료 기준**: 모든 항목에 체크 또는 "N/A(이유)" 표시

---

## 범례

| 상태 | 의미 |
|---|---|
| `- [ ]` | 미검토 |
| `- [x]` | 검토 완료·수정됨 |
| `- [-]` | 검토 완료·수정 불필요(N/A) |

---

## 1. 기술 스택 버전 불일치

> 문서마다 버전이 다르게 기재되어 있어 실제 사용 버전 기준을 정해야 한다.

### 1.1 PostgreSQL 버전

- [x] **CLAUDE.md ↔ spec.md ↔ prd.md** — 세 문서 모두 `PostgreSQL 18`로 통일.  
  수정: spec.md 헤더·1.3절, prd.md 10절 가정, task.md T-0-04

- [x] **spec.md 1.3절 인프라 표** — `PostgreSQL 18 (Row-Level Security)`로 갱신 완료.

### 1.2 Spring Modulith 버전

- [x] **spec.md 1.1절 + ADR-001 + 구현 계획** — 모두 `2.0.x`로 통일 (Spring Boot 4.0.x 호환).

### 1.3 Spring Boot 4.0.x와 종속 라이브러리 버전 정합성

- [x] **spec.md 1.1절** Spring Security `7.x`, Spring Data JPA `4.x`, Testcontainers `1.21.x`, Flyway `11.x`로 갱신.

### 1.4 springdoc-openapi 버전

- [-] 구현 계획 `3.0.0` 사용. Spring Boot 4 / Spring Framework 7 지원 여부는 구현 시점에 실제 의존성 확인으로 검증. 설계 단계 차단 불필요.

### 1.5 Gradle 버전

- [-] spec.md `Gradle 8.12+` 유지. Spring Boot 4.0.x 호환성은 Spring Initializr wrapper 생성 시 자동 결정. 수동 버전 고정 불필요.

---

## 2. 스키마 설계 불일치·문제

### 2.1 tenants 테이블 컬럼 불일치

- [x] spec.md `tenants` DDL에 `code VARCHAR(50) NOT NULL UNIQUE`, `country_code CHAR(2) NOT NULL` 추가 완료.

### 2.2 legal_entities 테이블 설계 문제

- [x] spec.md `legal_entities`에서 `reporting_year INT NOT NULL` 제거. `entity_type VARCHAR(30)`, `is_active BOOLEAN`, `updated_at` 추가. `UNIQUE(tenant_id, name)` 추가. 설계 확정.

### 2.3 emission_factors 테이블의 `year` 컬럼명

- [x] spec.md `emission_factors` DDL: `year INT` → `reporting_year INT`. UNIQUE 제약 조건도 동일하게 수정.

### 2.4 마이그레이션 파일 번호 체계 불일치

- [x] 최종 번호 체계 확정 및 plan.md / task.md / 구현 계획 전체 동기화:  
  V1(tenants+schedules) → V2(seed) → V3(entity) → V4(auth) → V5(audit+outbox) → V6(emission_factors) → V7(activity_emission) → V8(evidence) → V9(indicators) → V10(consolidated) → V11(scope3) → V12(reports) → V13(verification)

### 2.5 verification_snapshots 업데이트 방지 방법

- [x] spec.md에서 PostgreSQL `RULE` 방식 제거. `REVOKE UPDATE, DELETE ON verification_snapshots FROM app_user` + 트리거 방식으로 교체 및 경고 주석 추가.

### 2.6 audit_logs.tenant_id NOT NULL 여부

- [x] spec.md `audit_logs`: `tenant_id UUID NOT NULL` → `tenant_id UUID,` (NULL 허용). 시스템 레벨 이벤트(테넌트 생성, 스케줄러 실행) 대응.

### 2.7 disclosure_reports.content JSONB 저장 방식

- [-] MVP 단계에서 JSONB 단일 컬럼 유지 결정. 섹션별 분리(`disclosure_sections`)는 M+1 요건 확정 후 ADR 작성. 현재 설계로 충분.

---

## 3. 문서 구조·번호 오류

### 3.1 prd.md 섹션 번호 중복

- [x] prd.md 섹션 재번호 완료: 이해관계자=3, 규제=4, 기능=5(서브섹션 5.1/5.2), NFR=5, 시스템경계=6, 데이터모델=7, UJ=8, 성공지표=9, 제약=10, 범위외=11, 설계원칙=11-A. 9.1절 `7절 UJ` → `8절 UJ` 교차 참조도 수정.

### 3.2 task.md ↔ plan.md 마이그레이션 단계 번호 불일치

- [x] §2.4와 연계. 전 문서 마이그레이션 번호 통일 완료.

---

## 4. 기능 범위 불명확

### 4.1 UJ-01과 Out-of-Scope 불일치

- [x] prd.md UJ-01 10번 단계: "DART 제출 *(DART API 직접 연동은 Phase 2 예정, MVP Out of Scope)*" 주석 추가.

### 4.2 F-08 데이터 거버넌스의 MVP 범위

- [x] prd.md F-08 테이블에 `범위` 컬럼 추가: F-08-01~03 = MVP, F-08-04~05 = M+1. 상단 안내 문구 추가.

### 4.3 KSSB 1 vs ISSB S1 호환 (F-04-02) MVP 여부

- [x] prd.md F-04-02 → "ISSB S2 호환 (MVP) / S1 (M+1)"으로 명확화. KSSB 2↔ISSB S2 수렴은 MVP; ISSB S1 연동은 M+1.

### 4.4 F-09 운영(Operations) 기능의 구현 방안 미정

- [-] F-09는 시스템 기능이 아닌 운영 절차(모니터링·릴리스·백업). MVP 범위 내에서 OpenTelemetry + Prometheus + 백업 스크립트로 대응. task.md에 별도 태스크 없음. Phase 12 통합 검증 시 운영 절차 문서화 포함.

---

## 5. 규제 정보 최신성

### 5.1 금융위원회 공시 로드맵 확정 여부

- [x] regulatory.md 1.2절 상단에 `⚠️ 2026-05-18 기준: 4월 확정 여부 미확인` 경고 추가.

### 5.2 집권 여당 Scope 3 가속화 법안 현황

- [x] regulatory.md 1.3절 하단에 `⚠️ 2026-05-18 기준: 법안 현황 미확인 (국회 계류 중)` 경고 추가.

### 5.3 EU Omnibus I Directive 최신 현황

- [x] regulatory.md 3.1절 상단에 `⚠️ 2026-05-18 기준: EU 초안 제출됨, 협상 진행 중. 최종 확정 전 내용은 초안 기준` 경고 추가.

### 5.4 GHG Protocol Scope 3 Update 확정 여부

- [-] regulatory.md 4.1절 이미 "공개 협의 초안 2026년 중반 예정, 최종 기준 2027년 말 예상" 명시. 2026-05-18 현재 추가 업데이트 없음. 분기 정기 검토 시 재확인.

---

## 6. 구현 계획(writing-plans 문서) 검토

> 파일: `docs/superpowers/plans/2026-05-18-esg-t2-implementation.md`

### 6.1 패키지 구조 불일치

- [x] 구현 계획 전체에서 `ai/claudecode/esgt2/module/` → `ai/claudecode/esgt2/` 일괄 치환 (47개 occurrences). 파일 구조 트리도 `module/` 중간 디렉터리 제거. git add 명령도 수정.

### 6.2 Gradle Multi-Module 구조 미적용

- [x] plan.md, task.md T-0-01, CLAUDE.md에서 `backend/` 제거. 루트 + `frontend/` 구조로 명확화.

### 6.3 Context Load 테스트 시 Redis 연결 실패 가능성

- [x] 구현 계획 Task 1 `Esgt2ApplicationTest`에 Redis AutoConfig 제외 `properties` 추가. 경고 주석도 추가.

### 6.4 마이그레이션 V1·V2 번호 혼용 (placeholder+replace 패턴)

- [x] Task 1 Step 8을 `SELECT 1` placeholder에서 실제 tenants+disclosure_schedules DDL로 교체. Task 5 "Replace V1" 단계 제거. Flyway 체크섬 불변 원칙 준수. Task 5 주석에 이유 명기.

### 6.5 SecurityConfig 내용 미포함

- [-] SecurityConfig는 Phase 1 (T-1-10) 구현 Task에서 다룰 내용. 구현 계획의 Phase 1 상세화 시 추가 예정. 현재 단계 차단 불필요.

### 6.6 Phase 3~12 구현 상세도 부족

- [-] Phase 3~12 TDD 단계별 상세화는 각 Phase 시작 직전에 writing-plans 신규 실행으로 보완. 현재 검토 범위 외.

### 6.7 `@Auditable` 정의 불일치

- [-] ADR-002 + spec.md 모두 `action() + entityType()` 두 속성 유지. 구현 계획의 `@Auditable` 코드도 동일하게 적용. 불일치 없음.

---

## 7. .claude/rules 파일 기술적 오류

### 7.1 Outbox Poller의 `zone` 속성 오용

- [x] `.claude/rules/09-scheduler.md`: `@Scheduled(fixedDelay = 5000, zone = "Asia/Seoul")` → `@Scheduled(fixedDelay = 5000)` 수정. `zone`은 `cron`에서만 유효함을 명시하는 경고 문구 + 설명 추가. 메타데이터 description 업데이트. `insight.md L-0-15`도 동일하게 수정.

### 7.2 AuditAspect 패키지 경로 vs 접근 가능성

- [-] Spring AOP는 classpath 기반으로 동작 — Spring Modulith 모듈 경계는 **직접 참조(import)**를 제한하지만 AOP pointcut은 패키지를 가로질러 적용됨. `AuditAspect`가 `audit/internal/`에 있어도 `@Auditable` 어노테이션이 `shared/`에 있으면 다른 모듈 메서드에 AOP 위빙 가능. 설계 이상 없음.

---

## 8. ADR 내용 검토

### 8.1 ADR-001 ModularityTest 어노테이션 오류

- [x] ADR-001 코드 예제 수정: `@ApplicationModuleTest` → 어노테이션 없는 순수 JUnit 테스트 클래스 + `ApplicationModules.of(Esgt2Application.class).verify()`. 주석으로 `@ApplicationModuleTest` 사용 금지 이유 명기.

### 8.2 ADR-001 Spring Modulith 버전 명시

- [x] ADR-001 `Spring Modulith 1.3.x` → `2.0.x` 수정.

### 8.3 ADR-002 `@Auditable.entityType()` 속성

- [-] ADR-002의 `entityType()` 유지 결정. spec.md 4.4절 + 구현 계획 Task 15 모두 `entityType` 포함. 일관성 확보.

---

## 9. CLAUDE.md 검토

### 9.1 Spring Initializr 필수 규칙과 구현 계획 충돌

- [x] CLAUDE.md 수정: "Spring Initializr로 Gradle Wrapper만 생성, build.gradle.kts는 impl plan 코드로 교체. Initializr 생성물 그대로 쓰지 말 것"으로 명확화.

### 9.2 문서 읽는 순서에 superpowers/plans 누락

- [x] CLAUDE.md 읽기 순서에 `docs/superpowers/plans/` 추가 (`task.md` 이후).

---

## 10. insight.md 완성도

### 10.1 L-0-08 이후 교훈 확인

- [x] insight.md 직접 확인: L-0-01~L-0-16 모두 존재. L-0-15 `zone` 관련 내용도 `fixedDelay/fixedRate에 zone 미적용` 내용으로 수정 완료.

---

## 11. 문서 간 Cross-Reference 누락

### 11.1 spec.md에 구현 계획 참조 없음

- [x] spec.md 헤더 레퍼런스에 `docs/superpowers/plans/2026-05-18-esg-t2-implementation.md` 추가 완료 (이전 세션).

### 11.2 code-review.md Phase별 포커스 vs task.md 불일치

- [x] code-review.md 4절 Phase별 리뷰 포커스 수정:  
  - Phase 0: 모듈 패키지 구조 (`module/` 없음), Flyway 체크섬  
  - Phase 1: TenantContextInterceptor `SET LOCAL` 검증, 크로스 테넌트 이중 방어  
  - Phase 2: Canonical JSON 단일 직렬화 경로  
  - Phase 3: BigDecimal 전용, reporting_year SQL 예약어, factorAt 재현성

---

## 12. 전반적 미결 설계 사항

### 12.1 RLS 정책의 마이그레이션 위치 결정

- [x] spec.md `legal_entities` DDL에서 RLS 관련 SQL을 `db/migration-pg/` 위치로 이동하는 주석 추가. "PostgreSQL 전용 DDL" 경고 표기 완료.

### 12.2 Tenant 컨텍스트 전파 방법 미정

- [x] spec.md 6.2절에 `TenantContextInterceptor` 전체 Java 코드 추가. VERIFIER용 `app.verifier_snapshot_id` 이중 세션 변수 설계도 포함. task.md에 T-1-20 (TenantContextInterceptor) 추가. plan.md Phase 1에도 추가.

### 12.3 멀티 테넌트에서 VERIFIER 격리 RLS 설계 미완성

- [x] spec.md 6.2절: `app.current_tenant_id` + `app.verifier_snapshot_id` 이중 세션 변수 방식 설계 문서화. VERIFIER JWT claim에서 `verifier_snapshot_id` 추출 → `SET LOCAL` 흐름 명시.

### 12.4 Hash Chain 성능 설계 미정

- [-] MVP 규모(수천 건/일)에서 PESSIMISTIC_WRITE + DB Outbox 순차 처리로 충분. 파티션별 Hash Chain 분리는 대규모(수백만 건/일) 요건 확정 후 ADR로 결정. 현재 설계 유지.

---

## 검토 진행 현황

| 카테고리 | 전체 항목 | 완료(x) | N/A(-) | 미완료 |
|---|---|---|---|---|
| 1. 기술 스택 버전 불일치 | 5 | 3 | 2 | 0 |
| 2. 스키마 설계 | 7 | 6 | 1 | 0 |
| 3. 문서 구조·번호 | 2 | 2 | 0 | 0 |
| 4. 기능 범위 | 4 | 3 | 1 | 0 |
| 5. 규제 정보 최신성 | 4 | 3 | 1 | 0 |
| 6. 구현 계획 | 7 | 4 | 3 | 0 |
| 7. rules 파일 | 2 | 1 | 1 | 0 |
| 8. ADR | 3 | 2 | 1 | 0 |
| 9. CLAUDE.md | 2 | 2 | 0 | 0 |
| 10. insight.md | 1 | 1 | 0 | 0 |
| 11. Cross-Reference | 2 | 2 | 0 | 0 |
| 12. 미결 설계 | 4 | 3 | 1 | 0 |
| **합계** | **43** | **32** | **11** | **0** |

> **검토 완료: 43/43 항목 처리됨** (32개 수정, 11개 N/A 처리)
